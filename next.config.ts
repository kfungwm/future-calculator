/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  // basePath: '/way_construction', // De naam van je repository
  // assetPrefix: '/way_construction', // De naam van je repository
}

export default nextConfig
